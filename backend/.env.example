# PostgreSQL 接続URL
# 例: postgres://postgres:postgres@localhost:5432/fish_tech?sslmode=disable
DATABASE_URL=

# API起動ポート（未指定時: 8080）
PORT=8080

# 管理APIを許可するオリジン（カンマ区切り）
# 未指定時は http://localhost:3001 のみ許可
ADMIN_ALLOWED_ORIGINS=http://localhost:3001
